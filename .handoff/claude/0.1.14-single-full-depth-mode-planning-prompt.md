# Claude 规划提示词：journal-style 0.1.14 单一全量深度模式整改

你是 Claude / Fable，工作区为：

`/Users/a13497/Desktop/skill工作区/journal-style-skill`

请基于材料文件：

`/Users/a13497/Desktop/skill工作区/journal-style-skill/.handoff/claude/0.1.14-single-full-depth-mode-materials.md`

为 `journal-style` 生成一份严格、可执行、无补丁尾巴的整改计划。目标是把 skill 修成正式任务单一 full-depth 模式：所有正式期刊画像任务必须全量深度完成、全量跑完并成功交付；`metadata-only` 只能作为阻塞或中间状态，不能作为完成态、正式 C03 profile 或下游交付。

## 必须覆盖的问题面

1. 三模式口径清理
   - 移除或封存 `light` / `standard` 作为正式路径的能力。
   - 默认模式必须为 `full`。
   - 历史 `task-state.json` 或 `current-run-state.json` 中残留的 `standard` / `light` 不得让 runner 进入 metadata terminal。
   - 如保留 legacy/debug，只能用于离线回归，且必须禁止 production evidence。

2. 状态机与 gate
   - `config/workflow-states.json` 中正式 `run_modes` 只能指向 full-depth 路径。
   - `metadata_terminal` 不得作为正式任务终点。
   - 缺 MinerU/mu 完整全文包时，必须 fail-closed 停在 `step07b_mu_fulltext_pack`。
   - `step08a_metadata_layer` 作为 full-depth 中间证据保留，但不能跳到 handoff 完成。

3. C03 交付契约
   - C03 profile 只允许 Step10 完成后由文衡后端受控写入。
   - 必须有 `source_run_id`、H08 evidence、source lock 与 full-depth evidence path。
   - `metadata-only`、`standard-mode`、`production_profile=false` 不能被误称为交付成功。

4. `江汉论坛` 续跑策略
   - 现有事实：54 个 `full.md` 已非空，路径在 `/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/tmp/cnki/20260704-133757-query-d2130a/025-rag-import/fulltext/`。
   - 计划应要求从现有全文 sidecar 继续生成 full-depth 核心包、逐篇画像、聚合包、消费包、wenheng 状态与 C03 受控写回。
   - 不得重跑 CNKI/Zotero/PDF/RAG，除非真实证据证明现有 sidecar 不可用。

5. 回归与验收
   - 增加或修改回归 fixture：默认/旧 standard 状态必须进入 full，并在无全文包时停在 `step07b_mu_fulltext_pack`。
   - full-depth 完整 fixture 必须到 `completed`。
   - 禁止通过截图、手写 gate、手写 C03 source fields 或跳过状态机验收。
   - 验证命令必须包含 Python 编译、状态机 fixture、下游消费 fixture、sidecar 适配 fixture、release manifest check。

## 计划输出要求

- 计划按大步、小节、子步骤、验收标准、禁止方案、回滚点组织。
- 不写时间表，不写几周几个月计划。
- 不把任何已发现问题放入观察项或后期项；除非明确超出本 skill 边界，否则都要转成任务。
- 不允许中间总结性汇报；执行时按计划顺序连续推进。
- 发布前必须要求 Claude 终审，终审必须逐项核对材料文件、计划文件、代码 diff、测试输出和工作区状态。
