# journal-style 0.1.14 单一全量深度模式正式 Fable/Claude 复审提示词

你是本轮 `journal-style 0.1.14` 发布前的正式独立复审者。请在以下工作区执行复审：

`/Users/a13497/Desktop/skill工作区/journal-style-skill`

## 重要边界

1. 本文件用于正式 Fable/Claude 复审，不是 Codex 内部自审。
2. 工作区内已有的以下文件只能作为背景材料，不得作为 PASS 依据：
   - `.handoff/claude/0.1.14-single-full-depth-mode-final-review.md`
   - `.handoff/claude/0.1.14-single-full-depth-mode-rereview.md`
3. 上述两个文件是 Codex 内部预审/定向预审产物，不构成正式复审。你必须自行读代码、跑命令、查服务器证据、核验 C03 结果。
4. 未经用户明确授权，不得执行 commit、tag、push、formal skill sync、server runtime sync、production deploy。
5. 不得输出任何 secret、API key、token、cookie、私钥或含敏感值的环境变量。

## 必读文件

按顺序读取并对照：

1. Droid 整改计划：`.handoff/droid/0.1.14-single-full-depth-mode-plan.md`
2. 原始问题材料：`.handoff/claude/0.1.14-single-full-depth-mode-materials.md`
3. 本提示词：`.handoff/claude/0.1.14-single-full-depth-mode-formal-fable-review-prompt.md`
4. 进度日志：`progress.md`
5. 当前未提交 diff：`git diff`
6. 当前工作区状态：`git status --short`

## 输出文件

请把正式复审报告写入：

`.handoff/claude/0.1.14-single-full-depth-mode-formal-fable-review-result.md`

报告第一行只能是以下二选一：

`STATUS: PASS`

或：

`STATUS: BLOCKED`

若 `STATUS: BLOCKED`，必须列出阻断项编号、证据路径、违反计划原因、最小修复动作。不得用“基本通过”“建议后续观察”“非阻断但发布后处理”掩盖计划内缺口。

## 复审目标

确认 `journal-style 0.1.14` 是否已经完成以下全部事项，并且可以进入发布链：

- A 节：`validate_wenheng_status.py` 关闭 `overall_journal_style=metadata_only` 逃逸路径。
- B 节：新增并通过两个回归 fixture。
- C 节：江汉论坛真实任务从既有 54 篇非空 sidecar 全文续跑 full-depth 链，并通过文衡后端受控写入正式 C03 profile。
- D 节：release manifest 与当前 tracked scripts/config 一致。
- E 节：完整验收命令全过。
- F 节：全局禁止方案未被违反。
- BLOCK-01 后续修复：sidecar manifest 的 `reference_list` 覆盖必须与 mu fulltext pack 证据链一致，且 manifest 生成仍不打开 `full.md` 正文。

## A. 代码修复复审

检查 `scripts/validate_wenheng_status.py`：

- `ALLOWED_STATUS` 可以保留 `"metadata_only"`，但 `pipeline_status.overall_journal_style == "metadata_only"` 必须显式报错。
- 报错应指向 `overall_journal_style`，并说明 metadata-only 只能是阻塞/中间态，不能是完成态。

必须运行：

```bash
python3 -m py_compile scripts/validate_wenheng_status.py
python3 -c '
import json, tempfile, subprocess, sys, os
s = {
  "schema": "journal_style_wenheng_status_v1",
  "skill_id": "journal-style",
  "journal": {"name": "test", "identity_status": "pending"},
  "input": {"submission_title": "", "submission_abstract_path": "", "manuscript_path": "", "topic_keywords": [], "target_year_range": ""},
  "data_assets": {"title_list_path": "", "topic_library_path": "", "zotero_collections": [], "kb_ids": [], "official_evidence_path": ""},
  "pipeline_status": {"overall_journal_style": "metadata_only"},
  "metrics": {"confidence": "low"},
  "decision": {"recommended_action": "unknown"},
  "handoff": {},
  "updated_at": "2026-01-01T00:00:00Z"
}
tmp = tempfile.NamedTemporaryFile(mode="w", suffix=".json", delete=False)
json.dump(s, tmp); tmp.close()
r = subprocess.run([sys.executable, "scripts/validate_wenheng_status.py", "--input", tmp.name], capture_output=True, text=True)
os.unlink(tmp.name)
result = json.loads(r.stdout)
assert r.returncode == 1, (r.returncode, result)
assert any("overall_journal_style" in p for p in result["problems"]), result
print("A节修复验收: PASS")
'
```

## B. 回归 fixture 复审

检查 `tests/run_state_machine_fixtures.py`：

- 必须存在 `standard_task_forced_to_full_blocks_at_step07b`。
- 必须存在 `full_depth_with_valid_sidecar_completes`。
- 第一个 fixture 必须断言历史 `standard` 任务被解析为 `full` 并停在 `step07b_mu_fulltext_pack`。
- 第二个 fixture 必须断言完整 full-depth 链能到 `completed`，且不能跳过 scoring calibration。

必须运行：

```bash
python3 tests/run_state_machine_fixtures.py
```

验收：`ok=true`，`35/35`，且上述两个 fixture 均 PASS。

## C. 单一 full-depth 模式复审

检查：

- `config/workflow-states.json` 正式 `run_modes` 只有 `full`。
- 不存在正式 `metadata_terminal` 路径。
- `scripts/journal_style_runner.py` 不允许根据旧 `light` / `standard` 字段进入 metadata-only 终态。
- `scripts/build_task_skeleton.py` 只允许 `--run-mode full`。
- `SKILL.md`、`references/run-modes-protocol.md`、`references/output-schema.md`、`references/stage-gates-protocol.md`、`docs/wenheng-native-protocol.md` 均应说明 metadata-only 不是交付成功。

必须运行：

```bash
rg -n 'if mode in \["light", "standard"\]|elif mode == "standard"|metadata terminal|metadata-only 流程' scripts config SKILL.md references docs README.md
```

命中只能是禁止性说明、历史概念说明或测试用例；若命中正式执行分支，判 BLOCKED。

## D. Step6 输入/回执分离复审

检查 `config/workflow-states.json`：

- `step06_zotero_pdf_rag` 的原始输入必须是 `025-rag-import/zotero-pdf-rag-handoff-input.json`。
- `gate_input` 必须指向该 input 文件。
- `06-gates/zotero-pdf-rag-handoff.json` 只能是 gate receipt，不得再作为原始 handoff input 被 runner 覆写。

若同一路径同时承担 gate input 与 gate receipt，判 BLOCKED。

## E. sidecar manifest 与 mu fulltext pack 证据链复审

必须检查以下源码和测试：

- `scripts/build_mu_fulltext_core_pack_from_sidecar.py`
- `scripts/build_jiansuo_sidecar_manifest.py`
- `tests/run_sidecar_adaptation_fixtures.py`

验收点：

1. `build_mu_fulltext_core_pack_from_sidecar.py` 可以读取既有非空 `full.md`，生成 `mu-fulltext-core-pack.json`。
2. 同一脚本必须同时生成 `03-analysis/fulltext-layer/mu-fulltext-structure-summary.json`。
3. `mu-fulltext-structure-summary.json` 只能包含结构计数、sha、reference sources 等摘要字段，不得包含全文正文、paragraph text、RAG chunk、vector、embedding 或 secret。
4. `build_jiansuo_sidecar_manifest.py` 只能读取结构摘要做 count-only overlay，不得打开 `full.md` 正文来给 manifest 造 coverage。
5. manifest safety 必须保持 `full_md_files_opened=0`。
6. `tests/run_sidecar_adaptation_fixtures.py` 必须包含并通过 `structure_summary_reconciles_manifest_without_fullmd_read`。

必须运行：

```bash
python3 tests/run_sidecar_adaptation_fixtures.py
```

验收：`ok=true`，`9/9`。

## F. 江汉论坛真实任务复审

服务器路径：

- runtime：`/home/ubuntu/wenheng-runtime/journal-style-skill-0.1.14-runtime/`
- 运行任务：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704`
- 正式任务证据目录：`/opt/wenheng-control/repo/data/tasks/TASK-1783183335633-FULLDEPTH`
- sidecar：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/tmp/cnki/20260704-133757-query-d2130a/025-rag-import/fulltext`

必须核验，不得只引用 Codex 记录：

1. 54 个 sidecar `full.md` 非空。
2. 运行任务 `00-intake/jiansuo-sidecar-manifest.json`：
   - `item_count=54`
   - `ready_structure_count=54`
   - `section_tree.count=54`
   - `paragraph_sequence.count=54`
   - `reference_list.count=54`
   - `safety.full_md_files_opened=0`
3. 正式任务证据目录中的同名 manifest 也满足上述统计。
4. `03-analysis/fulltext-layer/mu-fulltext-core-pack.json` 存在且 `mu-fulltext-pack` gate 为 PASS，ready articles = 54。
5. `current-run-state.json` 为 `current_step=completed`，`blocked_reason=null`。
6. `05-handoff/journal-polish-consumption-pack.json` 为 `metadata_only=false`、`completion_label=FULLTEXT_READY`、`fulltext_layer_status=done`、`sample_count=54`。
7. `06-gates/provenance-required.json` verdict 为 PASS。
8. `scripts/validate_wenheng_status.py --input <task>/05-handoff/wenheng-center-status.json` 通过。

可使用类似命令，但必须由你实际运行并记录结果：

```bash
ssh beijing 'set -euo pipefail
cd /home/ubuntu/wenheng-runtime/journal-style-skill-0.1.14-runtime
TASK=/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704
python3 - <<PY
import json
from pathlib import Path
TASK=Path("/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704")
manifest=json.loads((TASK/"00-intake/jiansuo-sidecar-manifest.json").read_text(encoding="utf-8"))
state=json.loads((TASK/"current-run-state.json").read_text(encoding="utf-8"))
mu_gate=json.loads((TASK/"06-gates/mu-fulltext-pack.json").read_text(encoding="utf-8"))
prov=json.loads((TASK/"06-gates/provenance-required.json").read_text(encoding="utf-8"))
pack=json.loads((TASK/"05-handoff/journal-polish-consumption-pack.json").read_text(encoding="utf-8"))
print(json.dumps({
  "manifest_item_count": manifest["fulltext_index"].get("item_count"),
  "manifest_ready_structure_count": manifest["fulltext_index"].get("ready_structure_count"),
  "manifest_reference_count": manifest["fulltext_index"].get("full_mode_structure_coverage",{}).get("reference_list",{}).get("count"),
  "manifest_full_md_files_opened": manifest.get("safety",{}).get("full_md_files_opened"),
  "mu_gate_verdict": mu_gate.get("verdict"),
  "mu_ready_article_count": mu_gate.get("details",{}).get("ready_article_count"),
  "current_step": state.get("current_step"),
  "blocked_reason": state.get("blocked_reason"),
  "provenance_verdict": prov.get("verdict"),
  "consumption_completion_label": pack.get("completion_label"),
  "consumption_fulltext_layer_status": pack.get("fulltext_layer_status"),
  "consumption_sample_count": pack.get("sample_count")
}, ensure_ascii=False, indent=2))
PY
python3 scripts/validate_wenheng_status.py --input "$TASK/05-handoff/wenheng-center-status.json"'
```

## G. C03 正式写入复审

必须通过文衡后端真实数据核验：

- `profile_id = JP-TASK-1783183335633-FULLDEPTH`
- `task_id = TASK-1783183335633-FULLDEPTH`
- `source_run_id = RUN-jianghan-full-depth-20260706`
- `production_profile = true`
- `handoff_status = journal_style_task_completed`
- tags 包含 `FULLTEXT_READY`、`full-depth`、`54篇全文`、`参考文献生态`、`期刊画像`
- tags 不含 `metadata-only`、`standard-mode`

同时核验：

- `tasks` 表或后端任务读取中该任务为 `status=completed`、`mode=real`、`run_id=RUN-jianghan-full-depth-20260706`。
- 审计日志存在 `journal_profile_upserted_from_task`，对应该 task/profile。
- 写入来自后端 `/api/c03/journal-profiles/from-task/:taskId` 路径，不是人工补 source fields。

不得输出任何 API key 或数据库凭证。

## H. 全量本地验证命令

必须运行：

```bash
python3 -m py_compile scripts/*.py
python3 tests/run_state_machine_fixtures.py
python3 tests/run_downstream_consumable_fixtures.py
python3 tests/run_sidecar_adaptation_fixtures.py
python3 scripts/run_smoke_tests.py
python3 scripts/validate_readme.py
python3 scripts/validate_public_introduction.py --mode final
python3 scripts/build_release_manifest.py --check
git diff --check
```

全部 exit 0 才能 PASS。

## I. 工作区与发布边界

检查：

```bash
git status --short
git tag -l 'v0.1.14*'
realpath /Users/a13497/Desktop/skill工作区/journal-style-skill
realpath /Users/a13497/.codex/skills/journal-style
```

要求：

- 允许工作区仍是未提交候选，因为本复审是在发布前。
- 必须明确报告尚未执行 commit、tag、push、formal sync、server runtime final sync。
- 必须指出开发工作区与正式 Codex skill 安装路径不是同一份：  
  `/Users/a13497/Desktop/skill工作区/journal-style-skill`  
  `/Users/a13497/.codex/skills/journal-style`
- 若 PASS，只能写“允许进入用户授权后的发布链”，不能写“已经发布”。

## J. PASS 条件

只有以下全部成立才可 `STATUS: PASS`：

1. A-H 全部实际核验通过。
2. 江汉论坛 manifest / mu pack / runner / C03 四条证据链一致。
3. 没有把 Codex 内部预审报告当作正式复审证据。
4. 没有发现新的 light/standard/metadata-only 正式交付路径。
5. 没有发现手写 C03 source fields、跳过状态机、手写 gate verdict、重跑 CNKI/Zotero/PDF/RAG 或把 synthetic fixture 冒充正式画像。

若有任一项不满足，必须 `STATUS: BLOCKED`。
