# journal-style 0.1.14 单一全量深度模式发布前终审提示词

请对 `/Users/a13497/Desktop/skill工作区/journal-style-skill` 执行发布前终审。本次候选必须严格对照 Droid 计划：

`/Users/a13497/Desktop/skill工作区/journal-style-skill/.handoff/droid/0.1.14-single-full-depth-mode-plan.md`

## 审查结论格式

第一行只能写：

`STATUS: PASS`

或：

`STATUS: BLOCKED`

若 BLOCKED，必须列出阻断项编号、文件/证据位置、为何违反计划、最小修复动作。不得用“基本通过”“后续观察”替代阻断。

## 必须审查的材料

- 计划文件：`.handoff/droid/0.1.14-single-full-depth-mode-plan.md`
- 原始材料：`.handoff/claude/0.1.14-single-full-depth-mode-materials.md`
- 本终审提示词：`.handoff/claude/0.1.14-single-full-depth-mode-final-review-prompt.md`
- 进度日志：`progress.md`
- 代码与协议 diff：以当前工作区为准

## A. 单一全量深度模式

逐项确认：

1. `config/workflow-states.json` 的正式 `run_modes` 只有 `full`。
2. `scripts/journal_style_runner.py` 对历史 `light` / `standard` / 空模式均解析为 `full`，且无 metadata terminal 分支。
3. `scripts/build_task_skeleton.py` 新建任务只允许 `--run-mode full`。
4. 文档 `SKILL.md`、`references/run-modes-protocol.md`、`references/output-schema.md`、`references/stage-gates-protocol.md`、`docs/wenheng-native-protocol.md` 均明确：metadata-only 只能是阻塞/中间证据，不能作为交付成功。
5. `references/jiansuo-sidecar-consumption-protocol.md` 不再写“回退 metadata-only 流程”，而是要求缺 sidecar 时停在 MinerU/mu 全文包门禁。

必须实际运行或等价核验：

```bash
rg -n 'if mode in \["light", "standard"\]|elif mode == "standard"|metadata terminal|metadata-only 流程' scripts config SKILL.md references docs README.md
```

若发现正式路径仍能进入 `metadata-only` 交付，判 BLOCKED。

## B. validate_wenheng_status 逃逸路径

逐项确认：

1. `scripts/validate_wenheng_status.py` 显式拒绝 `pipeline_status.overall_journal_style == "metadata_only"`。
2. `ALLOWED_STATUS` 保留 `metadata_only` 只用于非 overall 字段的中间态，未误删。
3. 最小负向 JSON 会 exit 1，且 problems 指向 `overall_journal_style`。

必须运行：

```bash
python3 -m py_compile scripts/validate_wenheng_status.py
python3 -c '
import json, tempfile, subprocess, sys, os
s = {
  "schema": "journal_style_wenheng_status_v1",
  "skill_id": "journal-style",
  "journal": {"name": "test", "identity_status": "pending"},
  "input": {"submission_title": "", "submission_abstract_path": "", "manuscript_path": "", "topic_keywords": [], "target_year_range": ""},
  "data_assets": {"title_list_path": "", "topic_library_path": "", "zotero_collections": [], "kb_ids": [], "official_evidence_path": ""},
  "pipeline_status": {"overall_journal_style": "metadata_only"},
  "metrics": {"confidence": "low"},
  "decision": {"recommended_action": "unknown"},
  "handoff": {},
  "updated_at": "2026-01-01T00:00:00Z"
}
tmp = tempfile.NamedTemporaryFile(mode="w", suffix=".json", delete=False)
json.dump(s, tmp); tmp.close()
r = subprocess.run([sys.executable, "scripts/validate_wenheng_status.py", "--input", tmp.name], capture_output=True, text=True)
os.unlink(tmp.name)
result = json.loads(r.stdout)
assert r.returncode == 1, (r.returncode, result)
assert any("overall_journal_style" in p for p in result["problems"]), result
print("A节修复验收: PASS")
'
```

## C. 回归夹具

必须确认 `tests/run_state_machine_fixtures.py` 包含并通过：

- `standard_task_forced_to_full_blocks_at_step07b`
- `full_depth_with_valid_sidecar_completes`

必须运行：

```bash
python3 tests/run_state_machine_fixtures.py
```

验收：35/35 PASS，且上述两个 fixture 均显示 PASS。

## D. 检索入库 sidecar 到 mu 全文核心包

逐项确认：

1. `scripts/build_jiansuo_sidecar_manifest.py` 支持 `--sidecar-dir`，并统计 `ready_structure_count` 与 `full_mode_structure_coverage`。
2. 该脚本仍不打开 `full.md` 正文，安全计数 `full_md_files_opened` 必须保持 0。
3. `scripts/build_mu_fulltext_core_pack_from_sidecar.py` 从 `fulltext-index.json`、单篇 `manifest.json`、非空 `full.md` 和检索任务 metadata 构建 `03-analysis/fulltext-layer/mu-fulltext-core-pack.json`。
4. 禁止用 RAG chunks 反拼全文，禁止空 `full.md` 冒充全文。
5. `scripts/journal_style_runtime.py` 已把新增脚本纳入 release manifest 追踪。

必须运行：

```bash
python3 tests/run_sidecar_adaptation_fixtures.py
```

验收：8/8 PASS，其中 `build_mu_pack_from_sidecar_pass` 通过。

## E. Step6 gate 输入/回执分离

逐项确认：

1. `config/workflow-states.json` 中 Step6 输入为 `025-rag-import/zotero-pdf-rag-handoff-input.json`。
2. `06-gates/zotero-pdf-rag-handoff.json` 只作为 runner 生成的 gate receipt，不再被当成原始 handoff input 覆写。
3. `scripts/build_material_intake_manifest.py` 与相关 references 已同步新输入路径。

若仍存在同一路径既作为 gate input 又作为 gate receipt 的配置，判 BLOCKED。

## F. 江汉论坛真实任务副本全链

请核验以下真实执行事实，必要时登录服务器复查，但不得输出任何 secret：

- skill runtime 已同步到：`/home/ubuntu/wenheng-runtime/journal-style-skill-0.1.14-runtime/`
- 任务目录：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704`
- 检索入库 sidecar：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/tmp/cnki/20260704-133757-query-d2130a/025-rag-import/fulltext`
- 该任务未重跑 CNKI/Zotero/PDF/RAG，只消费既有 54 个非空 full.md sidecar。

必须确认：

1. `00-intake/jiansuo-sidecar-manifest.json` 统计为 54 篇，`section_tree` / `paragraph_sequence` / `reference_list` 结构覆盖均达到 54/54。
2. `03-analysis/fulltext-layer/mu-fulltext-core-pack.json` gate PASS，ready articles = 54。
3. runner 已推进到 `current_step: completed`，`blocked_reason: null`。
4. 产物存在且有效：
   - `03-analysis/fulltext-layer/per-article-style-profiles.json`
   - `03-analysis/fulltext-layer/journal-style-aggregation-bundle.json`
   - `04-fit-evaluation/journal-fit-scoring-model.json`
   - `05-handoff/journal-polish-consumption-pack.json`
   - `05-handoff/wenheng-center-status.json`
   - `05-handoff/handoff-to-downstream-skills.md`
   - `06-gates/provenance-required.json`
5. `validate_wenheng_status.py --input <task>/05-handoff/wenheng-center-status.json` 通过。

## G. C03 受控写入

必须核验文衡后端 C03 中存在新的正式画像：

- task id：`TASK-1783183335633-FULLDEPTH`
- profile id：`JP-TASK-1783183335633-FULLDEPTH`
- target/journal alias：`江汉论坛`
- source_run_id：`RUN-jianghan-full-depth-20260706`
- `production_profile: true`
- `handoff_status: journal_style_task_completed`
- tags 必须包含 `FULLTEXT_READY`、`full-depth`、`54篇全文`、`参考文献生态`、`期刊画像`
- tags 不得包含 `metadata-only`、`standard-mode`

该写入必须来自文衡后端 `/api/c03/journal-profiles/from-task/TASK-1783183335633-FULLDEPTH` 的受控调用，不得人工补 source fields。

## H. 全量验证命令

必须运行：

```bash
python3 -m py_compile scripts/*.py
python3 tests/run_state_machine_fixtures.py
python3 tests/run_downstream_consumable_fixtures.py
python3 tests/run_sidecar_adaptation_fixtures.py
python3 scripts/run_smoke_tests.py
python3 scripts/validate_readme.py
python3 scripts/validate_public_introduction.py --mode final
python3 scripts/build_release_manifest.py --check
git diff --check
```

全部 exit 0 才能 PASS。

## I. 发布边界

本次终审只允许给出是否可进入发布链的结论。未经用户再次明确授权，不得执行：

- commit
- tag
- push
- formal skill sync
- 生产部署
- 服务器 runtime 覆盖

若终审 PASS，请在报告中写明“允许进入发布链，但尚未发布”。
