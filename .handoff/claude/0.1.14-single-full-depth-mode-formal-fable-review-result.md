STATUS: PASS

# journal-style 0.1.14 单一全量深度模式 · 正式 Fable 复审报告

复审者：Fable（独立复审，非 Codex 内部自审）
复审时间：2026-07-06
工作区：/Users/a13497/Desktop/skill工作区/journal-style-skill
判定：**PASS，允许进入用户授权后的发布链（尚未发布）**

复审边界声明：本报告不采信 `.handoff/claude/0.1.14-single-full-depth-mode-final-review.md`、
`.handoff/claude/0.1.14-single-full-depth-mode-rereview.md` 两份 Codex 内部预审产物作为 PASS 依据；
所有结论均由本复审自行读码、跑命令、SSH 核验服务器证据、查询后端数据库得出。

---

## A 节 · validate_wenheng_status.py 逃逸路径关闭 — PASS

- 源码核验：`scripts/validate_wenheng_status.py` 在 `split_keys` 校验后新增对
  `pipeline_status.overall_journal_style == "metadata_only"` 的显式拦截，报错指向该字段并说明
  metadata-only 只能是阻塞/中间态，不能是完成态。`ALLOWED_STATUS` 保留 "metadata_only"（供其他中间字段合法使用）。
- `python3 -m py_compile scripts/validate_wenheng_status.py`：exit 0。
- 逃逸用例（overall_journal_style=metadata_only）：validator exit code 1，problems 含 "overall_journal_style"。
- 合法用例（overall_journal_style=blocked）：validator exit code 0，ok=true。
- 提示词 A 节内嵌验收脚本：输出 `A节修复验收: PASS`。

## B 节 · 回归 fixture 新增 — PASS

- `tests/run_state_machine_fixtures.py` 含 `standard_task_forced_to_full_blocks_at_step07b`：
  历史 standard 任务被解析为 run_mode=full，且停在 `step07b_mu_fulltext_pack`。断言成立。
- 含 `full_depth_with_valid_sidecar_completes`：完整 full-depth 链到达 `completed`，未跳过 scoring calibration。断言成立。
- `python3 tests/run_state_machine_fixtures.py`：ok=true，**35/35 PASS**，两个新 fixture 均 PASS。

## C 节 · 单一 full-depth 模式 — PASS

- `config/workflow-states.json`：正式 `run_modes` 只有 `full`，无 `metadata_terminal` 路径。
- `scripts/journal_style_runner.py`：`resolve_run_mode()` 无条件返回 "full"，不读旧 light/standard 字段选路径。
- `scripts/build_task_skeleton.py` 与 runner `--mode`：均限定 `choices=["full"]`。
- 文档全部说明 metadata-only 不是交付成功。
- `rg` 搜索（light/standard/metadata terminal 等）：仅 1 处命中，位于
  `references/stage-gates-protocol.md:33`，为禁止性说明（"必须解析为 full，不得进入 metadata terminal"），
  非正式执行分支。判定合规。

## D 节 · Step6 输入/回执分离 — PASS

- `step06_zotero_pdf_rag`：`requires_inputs` 与 `gate_input` 均指向
  `025-rag-import/zotero-pdf-rag-handoff-input.json`（原始输入）。
- gate receipt 写入 `06-gates/zotero-pdf-rag-handoff.json`，与 gate input 路径分离，不存在同路径既作输入又被 runner 覆写的情况。

## E 节 · sidecar manifest 与 mu fulltext pack 证据链 — PASS

- 三个文件均存在：`build_mu_fulltext_core_pack_from_sidecar.py`、`build_jiansuo_sidecar_manifest.py`、
  `tests/run_sidecar_adaptation_fixtures.py`。
- `build_mu_fulltext_core_pack_from_sidecar.py`：读取既有非空 full.md 生成 `mu-fulltext-core-pack.json`，
  同时生成 `03-analysis/fulltext-layer/mu-fulltext-structure-summary.json`（schema
  `journal_style_mu_fulltext_structure_summary_v1`）。摘要文件规则字段明确排除 mu 正文、paragraph text、
  RAG chunk、vector、secret（相关关键词仅出现在禁止性 "rule" 注释中，非实际写入字段）。
- `build_jiansuo_sidecar_manifest.py`：仅读结构摘要做 count-only overlay，manifest safety 保持
  `full_md_files_opened=0`。
- fixture `structure_summary_reconciles_manifest_without_fullmd_read` 存在并通过。
- `python3 tests/run_sidecar_adaptation_fixtures.py`：ok=true，**9/9 PASS**。

## F 节 · 江汉论坛真实任务（服务器实核，非引用 Codex 记录）— PASS

SSH 至 beijing 实核运行任务 `/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704`：

- sidecar full.md：总数 54，非空 54。
- `00-intake/jiansuo-sidecar-manifest.json`：item_count=54、ready_structure_count=54、
  section_tree.count=54、paragraph_sequence.count=54、reference_list.count=54、
  safety.full_md_files_opened=0。
- 正式任务证据目录 `/opt/wenheng-control/repo/data/tasks/TASK-1783183335633-FULLDEPTH` 同名 manifest：
  item_count/ready_structure/section_tree/paragraph/reference 均=54，full_md_files_opened=0。
- `03-analysis/fulltext-layer/mu-fulltext-core-pack.json` 存在；mu-fulltext-pack gate verdict=PASS，
  ready articles=54。
- `current-run-state.json`：current_step=completed，blocked_reason=null，run_mode=full。
- `05-handoff/journal-polish-consumption-pack.json`：metadata_only=false、completion_label=FULLTEXT_READY、
  fulltext_layer_status=done、sample_count=54。
- `06-gates/provenance-required.json`：verdict=PASS。
- `scripts/validate_wenheng_status.py --input <task>/05-handoff/wenheng-center-status.json`：ok=true，exit 0。

## G 节 · C03 正式写入（后端真实数据核验）— PASS

- C03 profile（journal-profiles-runtime.json）：
  profile_id=JP-TASK-1783183335633-FULLDEPTH、task_id=TASK-1783183335633-FULLDEPTH、
  source_run_id=RUN-jianghan-full-depth-20260706、production_profile=true、
  handoff_status=journal_style_task_completed。
  tags 含 FULLTEXT_READY、full-depth、54篇全文、参考文献生态、期刊画像；**不含** metadata-only、standard-mode。
- tasks 表（wenheng-runtime.sqlite）TASK-1783183335633-FULLDEPTH：
  **status=completed、mode=real、run_id=RUN-jianghan-full-depth-20260706**、progress=1.0。
- 审计日志 `data/logs/audit.jsonl` 存在 `journal_profile_upserted_from_task` 事件
  （2026-07-06T14:17:50.200Z），对应该 task/profile，证明写入来自后端
  `/api/c03/journal-profiles/from-task/:taskId` 受控路径，非人工补 source fields。
- 旧记录 JP-TASK-1783183335633-jianghan-forum-art-aesthetics-standard 仍在库中，
  但 production_profile=false、source_run_id=null、tags 含 metadata-only/standard-mode，
  按 C03 正式过滤规则被排除，不进入正式画像列表——符合设计意图，非新增逃逸路径。
- 未输出任何 API key、token、数据库凭证。

## H 节 · 全量本地验证命令 — PASS

- `python3 -m py_compile scripts/*.py`：exit 0。
- `python3 tests/run_state_machine_fixtures.py`：35/35 PASS。
- `python3 tests/run_downstream_consumable_fixtures.py`：23/23 PASS。
- `python3 tests/run_sidecar_adaptation_fixtures.py`：9/9 PASS。
- `python3 scripts/run_smoke_tests.py`：exit 0。
- `python3 scripts/validate_readme.py`：exit 0。
- `python3 scripts/validate_public_introduction.py --mode final`：exit 0。
- `python3 scripts/build_release_manifest.py --check`：exit 0（tracked scripts/config 与 manifest 一致，D 节亦成立）。
- `git diff --check`：exit 0。

## I 节 · 工作区与发布边界 — 确认

- `git status --short`：21 个 tracked 文件为未提交修改，另有若干 `.handoff/` 与
  `scripts/build_mu_fulltext_core_pack_from_sidecar.py` 未跟踪。属发布前候选状态，允许。
- `git tag -l 'v0.1.14*'`：无 v0.1.14* tag。
- 开发工作区 `/Users/a13497/Desktop/skill工作区/journal-style-skill` 与正式 Codex 安装路径
  `/Users/a13497/.codex/skills/journal-style` **不是同一份**。
- 尚未执行：commit、tag、push、formal skill sync、server runtime final sync、production deploy。

## J 节 · PASS 条件核对

1. A-H 全部实际核验通过 —— 成立。
2. 江汉论坛 manifest / mu pack / runner / C03 四条证据链一致（均以 54 篇为基准，
   status/mode/run_id/production_profile 一致）—— 成立。
3. 未采信 Codex 内部预审报告作为正式复审证据 —— 成立。
4. 未发现新的 light/standard/metadata-only 正式交付路径 —— 成立。
5. 未发现手写 C03 source fields、跳过状态机、手写 gate verdict、重跑 CNKI/Zotero/PDF/RAG、
   或 synthetic fixture 冒充正式画像 —— 成立（C03 来自后端受控路径且有审计事件；
   sidecar 续跑基于既有 54 篇非空 full.md，无重跑上游）。

---

## 结论

journal-style 0.1.14 单一全量深度模式整改已完成，A-H 全部核验通过，四条证据链一致，
无计划内缺口。**允许进入用户授权后的发布链**（commit / tag / push / formal sync / runtime sync
需另行获得用户明确授权，本复审未执行任何发布动作）。
