STATUS: PASS

# journal-style 0.1.14 BLOCK-01 定向复审

审查对象：`/Users/a13497/Desktop/skill工作区/journal-style-skill`

复审范围：上一轮 `STATUS: BLOCKED` 的唯一阻断项 `BLOCK-01`。本次只读源码、运行命令、核验证据；未改源码、未 commit、未 tag、未 push、未 deploy、未 formal sync。报告中不包含任何 secret。

## 1. BLOCK-01 解除确认

### 1.1 服务器运行任务 manifest

路径：

`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/00-intake/jiansuo-sidecar-manifest.json`

实测结果：

```json
{
  "item_count": 54,
  "ready_structure_count": 54,
  "section_tree_count": 54,
  "paragraph_sequence_count": 54,
  "reference_list_count": 54,
  "full_md_files_opened": 0
}
```

结论：通过。上一轮阻断中的 `reference_list.count=1` 已修复为 `54`，且 `full_md_files_opened` 仍为 `0`。

### 1.2 文衡正式任务证据目录 manifest

路径：

`/opt/wenheng-control/repo/data/tasks/TASK-1783183335633-FULLDEPTH/00-intake/jiansuo-sidecar-manifest.json`

实测结果：

```json
{
  "item_count": 54,
  "ready_structure_count": 54,
  "section_tree_count": 54,
  "paragraph_sequence_count": 54,
  "reference_list_count": 54,
  "full_md_files_opened": 0
}
```

结论：通过。正式任务证据目录与运行任务目录统计一致。

## 2. 实现方式核验

### 2.1 mu fulltext 结构摘要

两处任务目录均存在：

- `/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/03-analysis/fulltext-layer/mu-fulltext-structure-summary.json`
- `/opt/wenheng-control/repo/data/tasks/TASK-1783183335633-FULLDEPTH/03-analysis/fulltext-layer/mu-fulltext-structure-summary.json`

实测摘要：

```json
{
  "schema": "journal_style_mu_fulltext_structure_summary_v1",
  "article_count": 54,
  "first_counts": {
    "section_tree": 4,
    "paragraph_sequence": 61,
    "reference_list": 33
  },
  "first_reference_sources": ["derived_from_full_md_tail"]
}
```

结论：`build_mu_fulltext_core_pack_from_sidecar.py` 已产出 `mu-fulltext-structure-summary.json`，用于后续 manifest 的结构计数 overlay。

### 2.2 manifest 生成脚本不读取 full.md 正文

源码核验：

- `scripts/build_mu_fulltext_core_pack_from_sidecar.py` 读取既有 `full.md`，构建 `mu-fulltext-core-pack.json` 与 `mu-fulltext-structure-summary.json`。
- `scripts/build_jiansuo_sidecar_manifest.py` 通过 `load_structure_summary()` 读取 `03-analysis/fulltext-layer/mu-fulltext-structure-summary.json`。
- `scripts/build_jiansuo_sidecar_manifest.py` 的 `overlay_structure_summary()` 只读取 `structure_counts`，把 `section_tree`、`paragraph_sequence`、`reference_list` 的 count 作为结构覆盖 overlay。
- `scripts/build_jiansuo_sidecar_manifest.py` 仍只记录 `full_md_path` 指针，并在 manifest safety 中保持 `full_md_files_opened=0`。

结论：通过。新增实现不是由 `build_jiansuo_sidecar_manifest.py` 打开 `full.md` 给 manifest 造 coverage，而是先由 mu pack 构建脚本输出结构摘要，再由 manifest 脚本读取 count-only overlay。

### 2.3 回归 fixture

`tests/run_sidecar_adaptation_fixtures.py` 新增并通过：

`structure_summary_reconciles_manifest_without_fullmd_read`

该 fixture 验证 `reference_list` 缺失时可由结构摘要 reconcile 到 manifest，且 `full_md_files_opened=0`。

## 3. H 节全量验证命令

全部通过：

```text
python3 -m py_compile scripts/*.py
=> exit 0

python3 tests/run_state_machine_fixtures.py
=> ok=true, passed=35, total=35

python3 tests/run_downstream_consumable_fixtures.py
=> ok=true, passed=23, total=23

python3 tests/run_sidecar_adaptation_fixtures.py
=> ok=true, passed=9, total=9

python3 scripts/run_smoke_tests.py
=> smoke tests passed

python3 scripts/validate_readme.py
=> exit 0

python3 scripts/validate_public_introduction.py --mode final
=> exit 0

python3 scripts/build_release_manifest.py --check
=> release-manifest.json is current

git diff --check
=> exit 0
```

## 4. C03 无回归核验

通过后端 API `GET /api/c03/journal-profiles` 核验到正式画像仍存在且字段未回退：

```json
{
  "profile_id": "JP-TASK-1783183335633-FULLDEPTH",
  "journal_alias": "江汉论坛",
  "target_journal": "江汉论坛",
  "task_id": "TASK-1783183335633-FULLDEPTH",
  "source_run_id": "RUN-jianghan-full-depth-20260706",
  "production_profile": true,
  "handoff_status": "journal_style_task_completed",
  "tags": [
    "FULLTEXT_READY",
    "full-depth",
    "54篇全文",
    "参考文献生态",
    "期刊画像"
  ]
}
```

标签中未出现 `metadata-only` 或 `standard-mode`。

任务表核验：

```json
{
  "task_id": "TASK-1783183335633-FULLDEPTH",
  "task_type": "journal_style",
  "status": "completed",
  "mode": "real",
  "run_id": "RUN-jianghan-full-depth-20260706",
  "skill_id": "journal_style",
  "origin": "human"
}
```

审计日志存在 `journal_profile_upserted_from_task`，对应 `JP-TASK-1783183335633-FULLDEPTH` 与 `TASK-1783183335633-FULLDEPTH`。

## 5. 结论

上一轮唯一阻断项 `BLOCK-01` 已解除。H 节全量验证命令全部通过，C03 正式画像无回归。本轮定向复审结论为 `STATUS: PASS`。

本报告只代表发布前复审通过；尚未执行 commit、tag、push、deploy 或 formal sync。
