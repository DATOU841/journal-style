STATUS: BLOCKED

# journal-style 0.1.14 单一全量深度模式发布前终审

审查对象：`/Users/a13497/Desktop/skill工作区/journal-style-skill`

对照文件：

- 终审提示词：`.handoff/claude/0.1.14-single-full-depth-mode-final-review-prompt.md`
- Droid 计划：`.handoff/droid/0.1.14-single-full-depth-mode-plan.md`
- 材料文件：`.handoff/claude/0.1.14-single-full-depth-mode-materials.md`

本次审查只读源码、运行审查命令、核验证据；未提交、未 tag、未 push、未部署、未 formal sync。报告中不包含任何 secret。

## 阻断项

### BLOCK-01：江汉论坛真实任务 sidecar manifest 结构覆盖未达到终审提示词 F-1

证据位置：

- 服务器任务目录：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704`
- manifest：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/00-intake/jiansuo-sidecar-manifest.json`
- 检索入库 sidecar：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/tmp/cnki/20260704-133757-query-d2130a/025-rag-import/fulltext`

实测结果：

```json
{
  "item_count": 54,
  "ready_structure_count": 1,
  "full_mode_structure_coverage": {
    "section_tree": { "count": 54, "rate": 1.0 },
    "paragraph_sequence": { "count": 54, "rate": 1.0 },
    "reference_list": { "count": 1, "rate": 0.0185 }
  },
  "full_md_files_opened": 0
}
```

违反计划原因：

- 终审提示词 F-1 要求 `00-intake/jiansuo-sidecar-manifest.json` 统计为 54 篇，且 `section_tree` / `paragraph_sequence` / `reference_list` 结构覆盖均达到 54/54。
- 当前 manifest 的 `section_tree` 与 `paragraph_sequence` 为 54/54，但 `reference_list` 只有 1/54，`ready_structure_count` 只有 1。
- 后续 `mu-fulltext-core-pack.json` 已通过补充构建取得 54 篇 reference_list，并且 `mu-fulltext-pack` gate 已 PASS；但终审提示词明确要求核验的是 sidecar manifest 本身，因此不能用后续 mu pack 结果替代 F-1。

最小修复动作：

1. 按 Droid 计划 C-1/F-1 口径修正真实任务的 `00-intake/jiansuo-sidecar-manifest.json` 生成或更新流程，使该 manifest 对 54 篇文章的 `reference_list` 结构覆盖达到 54/54。
2. 保持 `full_md_files_opened: 0` 的安全边界；不得通过读取全文正文、RAG chunks 反拼或空字段冒充覆盖。
3. 重新在服务器任务目录核验 F-1、F-2、F-3、F-4、F-5，并重跑本报告 H 节全部验证命令。
4. 重新提交终审；本报告结论在该阻断解除前不得作为发布依据。

## 已通过项

### A. 单一全量深度模式

通过。

- `config/workflow-states.json` 的正式 `run_modes` 只有 `full`。
- `scripts/journal_style_runner.py` 的 `resolve_run_mode()` 对历史 `light` / `standard` / 空模式统一返回 `full`。
- `scripts/build_task_skeleton.py` 只允许 `--run-mode full`。
- 相关文档已经把 metadata-only 定位为阻塞/中间证据，而非交付成功。
- 搜索命令只命中文档中的禁止性说明，未命中正式执行路径的 metadata terminal 分支。

运行结果：

```text
rg -n 'if mode in \["light", "standard"\]|elif mode == "standard"|metadata terminal|metadata-only 流程' scripts config SKILL.md references docs README.md
=> 仅命中 references/stage-gates-protocol.md 中“不得进入 metadata terminal”的禁止性说明。
```

### B. validate_wenheng_status 逃逸路径

通过。

- `scripts/validate_wenheng_status.py` 保留 `ALLOWED_STATUS` 中的 `metadata_only`，用于非 overall 字段中间态。
- 同文件显式拒绝 `pipeline_status.overall_journal_style == "metadata_only"`。
- 最小负向 JSON exit 1，`problems` 指向 `overall_journal_style`。

运行结果：

```text
python3 -m py_compile scripts/validate_wenheng_status.py
=> exit 0

python3 -c '<overall_journal_style=metadata_only negative fixture>'
=> A节修复验收: PASS
```

### C. 回归夹具

通过。

`tests/run_state_machine_fixtures.py` 包含并通过：

- `standard_task_forced_to_full_blocks_at_step07b`
- `full_depth_with_valid_sidecar_completes`

运行结果：

```text
python3 tests/run_state_machine_fixtures.py
=> ok=true, passed=35, total=35
```

### D. 检索入库 sidecar 到 mu 全文核心包

通过。

- `scripts/build_jiansuo_sidecar_manifest.py` 支持 `--sidecar-dir`。
- 该脚本统计 `ready_structure_count` 与 `full_mode_structure_coverage`。
- 安全计数保持 `full_md_files_opened: 0`。
- `scripts/build_mu_fulltext_core_pack_from_sidecar.py` 存在，并已被 `scripts/journal_style_runtime.py` 与 `config/release-manifest.json` 追踪。
- sidecar 适配 fixture 通过，包括 `build_mu_pack_from_sidecar_pass`。

运行结果：

```text
python3 tests/run_sidecar_adaptation_fixtures.py
=> ok=true, passed=8, total=8

python3 scripts/build_release_manifest.py --check
=> release-manifest.json is current
```

### E. Step6 gate 输入/回执分离

通过。

`config/workflow-states.json` 中：

- Step6 输入为 `025-rag-import/zotero-pdf-rag-handoff-input.json`。
- `gate_input` 指向 `025-rag-import/zotero-pdf-rag-handoff-input.json`。
- `06-gates/zotero-pdf-rag-handoff.json` 只作为 gate receipt，不再作为原始 handoff input。

相关 references 已同步说明该分离规则。

### F. 江汉论坛真实任务副本全链

部分通过，但因 BLOCK-01 未完全满足。

已通过事实：

- skill runtime 存在：`/home/ubuntu/wenheng-runtime/journal-style-skill-0.1.14-runtime/`
- 任务目录存在：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704`
- 检索入库 sidecar 存在：`/home/ubuntu/wenheng-runtime/jianghan-forum-journal-style-20260704/tmp/cnki/20260704-133757-query-d2130a/025-rag-import/fulltext`
- 54 个 `full.md` 均为非空：`total=54, nonempty=54, empty=0`
- `03-analysis/fulltext-layer/mu-fulltext-core-pack.json` 对 54 篇文章构建完成，且 `reference_list` 非空文章数为 54。
- `06-gates/mu-fulltext-pack.json` verdict 为 `PASS`，`ready_article_count=54`。
- runner 状态为 `current_step: completed`，`blocked_reason: null`，`run_mode: full`。
- 以下产物均存在且非空：
  - `03-analysis/fulltext-layer/per-article-style-profiles.json`
  - `03-analysis/fulltext-layer/journal-style-aggregation-bundle.json`
  - `04-fit-evaluation/journal-fit-scoring-model.json`
  - `05-handoff/journal-polish-consumption-pack.json`
  - `05-handoff/wenheng-center-status.json`
  - `05-handoff/handoff-to-downstream-skills.md`
  - `06-gates/provenance-required.json`
- `journal-polish-consumption-pack.json`：`metadata_only=false`，`completion_label=FULLTEXT_READY`，`fulltext_layer_status=done`，`sample_count=54`。
- `06-gates/provenance-required.json`：`verdict=PASS`，`problems=[]`。
- 服务器端 `validate_wenheng_status.py --input .../05-handoff/wenheng-center-status.json` 通过，输出 `ok=true, problems=[]`。

未通过事实：

- `00-intake/jiansuo-sidecar-manifest.json` 的 `reference_list` 覆盖仍为 1/54，不满足 F-1。

### G. C03 受控写入

通过。

通过后端 API `GET /api/c03/journal-profiles` 核验到正式画像：

```json
{
  "profile_id": "JP-TASK-1783183335633-FULLDEPTH",
  "journal_alias": "江汉论坛",
  "target_journal": "江汉论坛",
  "task_id": "TASK-1783183335633-FULLDEPTH",
  "source_run_id": "RUN-jianghan-full-depth-20260706",
  "production_profile": true,
  "handoff_status": "journal_style_task_completed",
  "tags": [
    "FULLTEXT_READY",
    "full-depth",
    "54篇全文",
    "参考文献生态",
    "期刊画像"
  ]
}
```

标签中未出现 `metadata-only` 或 `standard-mode`。

受控写入来源核验：

- 后端服务源码中 `/api/c03/journal-profiles/from-task/:taskId` 调用 `upsertProfileFromTask()`，该路径从 task 派生 `task_id`、`source_run_id`、`evidence_path`、`production_profile` 等 source fields。
- 服务器 `data/logs/audit.jsonl` 存在事件：`journal_profile_upserted_from_task`，数据为 `profile_id=JP-TASK-1783183335633-FULLDEPTH`、`task_id=TASK-1783183335633-FULLDEPTH`。
- `tasks` 表中 `TASK-1783183335633-FULLDEPTH` 为 `task_type=journal_style`、`status=completed`、`mode=real`、`run_id=RUN-jianghan-full-depth-20260706`。

### H. 全量验证命令

全部通过。

```text
python3 -m py_compile scripts/*.py
=> exit 0

python3 tests/run_state_machine_fixtures.py
=> ok=true, passed=35, total=35

python3 tests/run_downstream_consumable_fixtures.py
=> ok=true, passed=23, total=23

python3 tests/run_sidecar_adaptation_fixtures.py
=> ok=true, passed=8, total=8

python3 scripts/run_smoke_tests.py
=> smoke tests passed

python3 scripts/validate_readme.py
=> exit 0

python3 scripts/validate_public_introduction.py --mode final
=> exit 0

python3 scripts/build_release_manifest.py --check
=> release-manifest.json is current

git diff --check
=> exit 0
```

## 发布边界

当前结论为 BLOCKED，不允许进入发布链。不得 commit、tag、push、formal skill sync、生产部署或服务器 runtime 覆盖，直到 BLOCK-01 修复并复审 PASS。
